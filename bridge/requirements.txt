# Desktop bridge. The phone needs none of this — it ships the model.
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0

# Only needed for iqoo_mcp_server.py. Both major versions work — 2.x renamed
# FastMCP to MCPServer and the server aliases whichever one is installed.
mcp>=1.2
