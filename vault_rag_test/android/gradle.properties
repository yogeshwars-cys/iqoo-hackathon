org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin's incremental compiler cannot close its memory-mapped .tab caches on
# this Windows setup, failing :android_file_picker:compileDebugKotlin with
# "Could not close incremental caches ... class-fq-name-to-source.tab".
# Plugin modules are compiled once and cached by Gradle anyway, so incremental
# Kotlin compilation buys nothing here.
kotlin.incremental=false
